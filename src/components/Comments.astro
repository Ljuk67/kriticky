---
// Client-only Supabase comments (moderated):
// - Reads PUBLIC_SUPABASE_URL and PUBLIC_SUPABASE_ANON_KEY
// - Lists approved comments for the given slug
// - Submits new comments; DB handles approval/defaults via RLS/defaults
// UI text is Slovak; code identifiers in English
interface Props { slug: string; }
const { slug } = Astro.props as Props;
import commentsClientUrl from '../assets/comments-client.js?url';
const canRun = Boolean(import.meta.env.PUBLIC_SUPABASE_URL && import.meta.env.PUBLIC_SUPABASE_ANON_KEY);
---

<section class="comments" aria-label="Komentáre">
  <h3> 💬 Diskusia</h3>
  <h5>Pridať komentár:</h5>

  {!canRun && (
    <p class="note">Komentáre sú dočasne vypnuté. Chýba <code>PUBLIC_SUPABASE_URL</code> alebo <code>PUBLIC_SUPABASE_ANON_KEY</code>.</p>
  )}

  {canRun && (
    <>
      <form id="comment-form" method="POST" action="javascript:void(0)" onsubmit="return false" novalidate>
        <input type="hidden" name="slug" value={slug} />
        <p>
          <label>Meno
            <input name="name" required maxlength="80" />
          </label>
        </p>
        <p>
          <label>E‑mail
            <input type="email" required name="email" />
          </label>
        </p>
        <p class="hp">
          <label>Nepoužívaj toto pole
            <input name="hp_field" tabindex="-1" autocomplete="off" />
          </label>
        </p>
        <p>
          <label>Komentár
            <textarea name="message" required minlength="10" maxlength="5000"></textarea>
          </label>
        </p>
        <p>Tento formulár ukladá Váš email a meno len na antispamové opatrenia. Pre požiadanie o ich anonymizáciu alebo odstránenie <a href="/kontakt/" class="text-white" target="_blank">stačí napísať</a>.</p>
        <button type="button">Odoslať komentár</button>
        <span id="form-status" role="status" aria-live="polite"></span>
      </form>
      
      <div id="comments-list" data-slug={slug} data-supabase-url={import.meta.env.PUBLIC_SUPABASE_URL} data-supabase-key={import.meta.env.PUBLIC_SUPABASE_ANON_KEY}>
        <div class="loading">Načítavam komentáre…</div>
      </div>

      <script type="module" src={commentsClientUrl}></script>
      
    </>
  )}

  <style>
    .comments { margin-top: 2rem; background: var(--accent); padding: 2rem 3rem; color: white; border-radius: 18px;}
    .comments :is(h2, h3, h4, h5, h6){ margin-bottom: 0.5rem; color: white;text-align: center; }
    .loading, .muted { color: rgb(var(--gray)); }
    .error { color: #e11d48; }
    /* Center and size the form and list: fill parent, no fixed px max */
    form#comment-form, #comments-list { width: 100%; max-width: 100%; margin: 0 auto; box-sizing: border-box; }
    #comments-list { width: 100%; max-width: 100%; margin-inline: auto; box-sizing: border-box; margin: 1rem auto;  background: var(--accent-dark); border-radius: 12px;padding: 20px;}
    
    /* Inputs */
    form#comment-form p { margin: 0.75rem 0; width: 100%; display: block; }
    form#comment-form input, form#comment-form textarea {
      width: 100%;
      padding: 13px;
      border: none;
      border-radius: 12px;
      background: #f2f2f2;
      box-sizing: border-box; /* prevent right-side bleed from padding */
    }
    form#comment-form textarea { height: 150px; }
    form#comment-form .hp { position: absolute; left: -9999px; opacity: 0; height: 0; }
    #form-status { margin-left: 0.75rem; color: rgb(var(--white));}
    
    /* Submit button */
    form#comment-form button { padding: 10px 15px; color: white; background: black; border: 0; border-radius: 6px; font-size: 1em; cursor: pointer; }
    
    /* Comments list items are injected via JS, so use :global to style them */
    :global(.comments .comment) { padding: 0.75rem 0; border-bottom: 1px solid rgba(0,0,0,0.08); }
    :global(.comments .comment .meta) { color: var(--secondary); padding-left: 25px; font-size: 0.9rem; margin-bottom: 0.25rem; }
    :global(.comments .comment .body) { white-space: pre-wrap; }
    
    /* Small screens */
    @media (max-width: 480px) {
      .comments { padding: 18px; }
      form#comment-form textarea { height: 120px; }
    }
    
  </style>
</section>
